<div class="contents row">
  <div class="container">
  <%= form_with(model: @tweet, local: true) do |form| %>
    <h3>投稿する</h3>
    <%= render 'layouts/error_messages', model: form.object %>
    <%= render partial: "from", locals: {tweet: @tweet} %>
    <%#= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>